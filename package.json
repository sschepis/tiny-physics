{
  "name": "@sschepis/tiny-physics",
  "version": "0.0.4",
  "description": "A tiny javascript physics engine",
  "main": "dist/tiny-physics.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --outfile=dist/tiny-physics.min.js --minify --sourcemap",
    "watch": "esbuild src/index.js --bundle --outfile=dist/tiny-physics.min.js --sourcemap --watch"
  },
  "keywords": [
    "physics",
    "javascript",
    "engine"
  ],
  "author": "Sebastian Schepis",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.12"
  }
}
